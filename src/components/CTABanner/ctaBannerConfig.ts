import type { CTABannerConfig } from "./ctaBannerTypes";

export const CTA_BANNER_DEFAULTS: CTABannerConfig = {
  heading: "Your brand belongs up there.",
  sub: "Talk to the team that owns the skyline.",
  button: { label: "Start a Campaign", href: "/contact" },
};

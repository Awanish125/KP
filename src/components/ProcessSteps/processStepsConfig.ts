import type { ProcessStepsConfig } from "./processStepsTypes";

export const PROCESS_STEPS_DEFAULTS: ProcessStepsConfig = {
  label: "How It Works",
  heading: "Brief to billboard in four moves.",
};

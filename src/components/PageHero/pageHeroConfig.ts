import type { PageHeroConfig } from "./pageHeroTypes";

/** Fallbacks only — every page passes its own copy from src/data/*.json. */
export const PAGE_HERO_DEFAULTS: PageHeroConfig = {
  label: "Kiran Publicity",
  line1: "OUTDOOR",
  line2: "ADVERTISING",
  sub: "",
};
